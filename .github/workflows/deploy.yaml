name: Validate and deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v4
        with:
          version: 11
      - uses: actions/setup-node@v6
        with:
          node-version: '24.x'
          cache: pnpm
      - run: pnpm install --frozen-lockfile
        env:
          PNPM_CONFIG_BLOCK_EXOTIC_SUBDEPS: 'false'
      - name: Verify published generator
        run: |
          node <<'JS'
          const assert = require('node:assert/strict');
          const pkg = require('ergogen/package.json');
          assert.equal(pkg.name, '@runinit/ergogen');
          assert.equal(pkg.version, '5.0.0');
          JS
      - run: pnpm run test:release
      - run: pnpm run precommit
      - run: git diff --exit-code
      - run: pnpm run build
        env:
          REACT_APP_GTAG_ID: ${{ vars.REACT_APP_GTAG_ID }}
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm run test:e2e
      - name: Prepare Pages routing
        run: cp dist/index.html dist/404.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: check
    if: github.repository == 'runinit/ergogen-gui' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment
